<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="approval_purchase_request_view_form_inherit_button" model="ir.ui.view">
        <field name="name">approval.purchase.request.view.form.inherit.button</field>
        <field name="model">approval.request</field>
        <field name="inherit_id" ref="approvals.approval_request_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_create_purchase_orders']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//button[@name='action_confirm']" position="after">
                <button string="Create RFQ's" name="%(x_purchase_type_approval.action_generate_approval_request)d" type="action" invisible="approval_type != 'purchase' or request_status != 'approved' or purchase_order_count &gt; 0" />
            </xpath>

        </field>
    </record>
</odoo>
